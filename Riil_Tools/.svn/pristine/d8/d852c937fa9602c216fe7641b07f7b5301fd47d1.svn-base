/*package com.riil.base.policy.impl;

import com.riil.base.policy.IPolicyListener;
import com.riil.base.resmodel.pojo.policy.PolicyPojo;

public class PolicyListenerWrap implements EntryListener<String, PolicyPojo>{

	IPolicyListener listener;
	
	public PolicyListenerWrap(IPolicyListener listener){
		this.listener = listener;
	}
	
	@Override
	public void entryAdded(EntryEvent<String, PolicyPojo> event) {
		if(listener==null) return;
		listener.policyAdded(event.getValue());
	}

	@Override
	public void entryRemoved(EntryEvent<String, PolicyPojo> event) {
		if(listener==null) return;
		listener.policyRemoved(event.getValue());
	}

	@Override
	public void entryUpdated(EntryEvent<String, PolicyPojo> event) {
		if(listener==null) return;
		listener.policyUpdated(event.getValue());
	}
	
	@Override
	public void entryEvicted(EntryEvent<String, PolicyPojo> event) {
		//nothing to do
	}
	
}
*/