<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.riil.base.resmodel.impl.db.dao.ModelBaseDao">
	<sql id="limit_one">
		and rownum = 1
	</sql>
	<sql id="page_head">
	SELECT * FROM (	SELECT TMP.*, rownum rn FROM (
	</sql>
	
	<sql id="page_foot">
		) TMP WHERE rownum &lt; ${pageSize}+${startIndex} ) WHERE rn &gt;= ${startIndex}
	</sql>
	
	<!-- CONCAT(CONCAT('%', #{desc ,jdbcType=VARCHAR}), '%')  -->
	<sql id="func_concat_1">CONCAT(CONCAT('%',</sql><sql id="func_concat_2">), '%')</sql>	
</mapper>
