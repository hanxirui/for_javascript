<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.riil.base.resmodel.impl.db.dao.VendorDao">

	<resultMap id="vendorResultMap" type="com.riil.base.resmodel.pojo.base.VendorPojo">
		<result property="id" column="C_ID" javaType="string" jdbcType="VARCHAR" />
		<result property="name" column="C_NAME" javaType="string"
			jdbcType="VARCHAR" />
		<result property="sortId" column="C_SORT_ID" javaType="_int"
			jdbcType="SMALLINT" />
		<result property="tag1" column="C_TAG1" javaType="string"
			jdbcType="VARCHAR" />
		<result property="tag2" column="C_TAG2" javaType="string"
			jdbcType="VARCHAR" />
		<result property="tag3" column="C_TAG3" javaType="string"
			jdbcType="VARCHAR" />
		<result property="tag4" column="C_TAG4" javaType="string"
			jdbcType="VARCHAR" />
		<result property="vendorIcon" column="C_VENDOR_ICON" javaType="string"
			jdbcType="VARCHAR" />
	</resultMap>

	<sql id="Query_Where_Clause">
		<trim prefix="WHERE" prefixOverrides="AND |OR ">
			<if test="id != null and id != ''">
				AND C_ID = #{id}
			</if>

			<if test="name != null and name != ''">
				AND C_NAME = #{name}
			</if>

			<if test="sortId != 0 ">
				AND C_SORT_ID = #{sortId}
			</if>

			<if test="tag1 != null and tag1 != ''">
				AND C_TAG1 = #{tag1}
			</if>

			<if test="tag2 != null and tag2 != ''">
				AND C_TAG2 = #{tag2}
			</if>

			<if test="tag3 != null and tag3 != ''">
				AND C_TAG3 = #{tag3}
			</if>

			<if test="tag4 != null and tag4 != ''">
				AND C_TAG4 = #{tag4}
			</if>

			<if test="vendorIcon != null and vendorIcon != ''">
				AND C_VENDOR_ICON = #{vendorIcon}
			</if>

		</trim>
	</sql>

	<!-- Insert POJO object -->
	<insert id="insert" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo">
		INSERT INTO t_moni_vendor ( C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3,
		C_TAG4, C_VENDOR_ICON )
		VALUES( #{id}, #{name}, #{sortId}, #{tag1}, #{tag2}, #{tag3}, #{tag4},
		#{vendorIcon} )
    </insert>
	
	<!--batch Insert POJO object -->
	<insert id="batchInsert" parameterType="list">
		INSERT INTO t_moni_vendor ( C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3,
		C_TAG4, C_VENDOR_ICON )
		VALUES
		<foreach collection="list" item="item" separator=",">
			( #{item.id}, #{item.name}, #{item.sortId}, #{item.tag1}, #{item.tag2}, #{item.tag3}, #{item.tag4},
		#{item.vendorIcon} )
		</foreach>
	</insert>
	
	
	<!-- Update POJO object by ID -->
	<update id="update_by_id" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo">
		UPDATE t_moni_vendor
		SET C_ID = #{id},
		C_NAME = #{name},
		C_SORT_ID = #{sortId},
		C_TAG1 = #{tag1},
		C_TAG2 = #{tag2},
		C_TAG3 = #{tag3},
		C_TAG4 = #{tag4},
		C_VENDOR_ICON = #{vendorIcon}
		WHERE C_ID = #{id}
    </update>

	<!-- Delete POJO object by ID -->
	<delete id="delete_by_id" parameterType="string">
		DELETE FROM t_moni_vendor WHERE C_ID = #{id}
    </delete>

	<!-- Delete all POJO object -->
	<delete id="delete_all">
		DELETE FROM t_moni_vendor
    </delete>

	<!-- Delete POJO object by query -->
	<delete id="delete_by_query" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo">
		DELETE FROM t_moni_vendor
		<include refid="Query_Where_Clause" />
	</delete>

	<!-- Select POJO object by ID -->
	<select id="select_by_id" parameterType="string" resultMap="vendorResultMap">
		SELECT C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3, C_TAG4,
		C_VENDOR_ICON
		FROM t_moni_vendor
		WHERE C_ID = #{id}
    </select>

	<!-- Select POJO object by query -->
	<select id="select_one_by_query" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo"
		resultMap="vendorResultMap">
		SELECT C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3, C_TAG4,
		C_VENDOR_ICON
		FROM t_moni_vendor
		<include refid="Query_Where_Clause" />
	</select>

	<!-- Select All POJO object -->
	<select id="select_all" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo"
		resultMap="vendorResultMap">
		SELECT C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3, C_TAG4,
		C_VENDOR_ICON
		FROM t_moni_vendor
		<trim prefix="ORDER BY">
			<if test="orderBy != null and orderBy != ''">
				${orderBy}
			           </if>
		</trim>
	</select>

	<!-- Select POJO object list by query -->
	<select id="select_list_by_query" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo"
		resultMap="vendorResultMap">
		SELECT C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3, C_TAG4,
		C_VENDOR_ICON
		FROM t_moni_vendor
		<include refid="Query_Where_Clause" />
		<trim prefix="ORDER BY">
			<if test="orderBy != null and orderBy != ''">
				${orderBy}
			           </if>
		</trim>
	</select>

	<!-- Select POJO object collection for page by query -->
	<select id="select_pagelist_by_query_count" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo"
		resultType="int">
		SELECT count(*)
		FROM t_moni_vendor
		<include refid="Query_Where_Clause" />
	</select>
	
	<select id="select_pagelist_by_query" parameterType="com.riil.base.resmodel.pojo.base.VendorPojo"
		resultMap="vendorResultMap">
		SELECT C_ID, C_NAME, C_SORT_ID, C_TAG1, C_TAG2, C_TAG3, C_TAG4,
		C_VENDOR_ICON
		FROM t_moni_vendor
		<include refid="Query_Where_Clause" />
		<trim prefix="ORDER BY">
			<if test="orderBy != null and orderBy != ''">
				${orderBy}
			           </if>
		</trim>
		limit ${startIndex}, ${pageSize}
	</select>
</mapper>
